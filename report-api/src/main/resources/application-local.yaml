spring:
  config:
    activate:
      on-profile: local
oauth2:
  security:
    enabled: false
eureka:
  instance:
    preferIpAddress: true
  client:
    fetchRegistry: false
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://discovery-service:8080/eureka/